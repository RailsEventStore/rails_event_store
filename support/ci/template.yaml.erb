name: <%= name %>
on:
  repository_dispatch:
    types:
      - script
  workflow_dispatch:
  push:
    paths:
      <%= paths %>
  pull_request:
    types: [opened, reopened]
    paths:
      <%= paths %>
jobs:
  <%= job %>