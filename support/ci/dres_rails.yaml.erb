name: <%= name %>
on:
  repository_dispatch:
    types:
      - script
  workflow_dispatch:
  push:
    paths:
      - "contrib/dres_client/**"
      - "contrib/dres_rails/**"
      - ".github/workflows/<%= name %>.yml"
      - "support/**"
      - "!support/bundler/**"
      - "!support/ci/**"
  pull_request:
    types: [opened, reopened]
    paths:
      - "<%= working_directory %>/**"
      - "contrib/dres_client/**"
      - ".github/workflows/<%= name %>.yml"
      - "support/**"
      - "!support/bundler/**"
      - "!support/ci/**"
jobs:
  <%= job_name %>:
    runs-on: ubuntu-20.04
    env:
      <%= env %>
    services:
      <%= services %>
    strategy:
      fail-fast: false
      matrix:
        include:
          <%= matrix %>
    steps:
      <%= steps %>