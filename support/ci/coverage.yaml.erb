name: <%= name %>
on:
  repository_dispatch:
    types:
      - script
  schedule:
    - cron: "0 17 * * *"
  workflow_dispatch:
  push:
    paths:
      <%= paths %>
  pull_request:
    types: [opened, reopened]
    paths:
      <%= paths %>
jobs:
  <%= job %>