name: <%= name %>
on:
  workflow_dispatch:
  repository_dispatch:
    types:
      - script
  push:
    paths:
      <%= paths %>
  pull_request:
    types: [opened, reopened]
    paths:
      <%= paths %>
  schedule:
    - cron: "0 17 * * *"
jobs:
  <%= job %>