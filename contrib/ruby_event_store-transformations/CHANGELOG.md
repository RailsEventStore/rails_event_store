### 0.2.0

- Change: Allow `ruby_event_store` 3.x by relaxing the dependency bound to `< 4.0`. The gem code is unchanged; this release only updates the dependency metadata so the gem can be installed alongside RubyEventStore 3.0.

### 0.1.0

- Initial release
