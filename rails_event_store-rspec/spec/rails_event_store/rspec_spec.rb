require "spec_helper"

RSpec.describe RailsEventStore::RSpec do
  it "has a version number" do
    expect(RailsEventStore::RSpec::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
