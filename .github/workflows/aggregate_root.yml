name: aggregate_root
on:
  repository_dispatch:
    types:
      - script
  workflow_dispatch:
  push:
    paths-ignore:
      - "railseventstore.org/**"
      - "contrib/**"
  pull_request:
    types: [opened, reopened]
    paths-ignore:
      - "railseventstore.org/**"
      - "contrib/**"
jobs:
  test:
    runs-on: [self-hosted]
    env:
      WORKING_DIRECTORY: aggregate_root
      BUNDLE_GEMFILE: ${{ matrix.gemfile }}
      ImageOS: ubuntu22
    strategy:
      fail-fast: false
      matrix:
        include:
          - ruby: ruby_3_1
            gemfile: Gemfile
          - ruby: ruby_3_0
            gemfile: Gemfile
          - ruby: ruby
            gemfile: Gemfile
    steps:
      - uses: actions/checkout@v3
      - run: test -e ${{ matrix.gemfile }}.lock
        working-directory: ${{ env.WORKING_DIRECTORY }}
      - run: echo $LD_LIBRARY_PATH
      - run: env
      - run: nix-shell -p ${{ matrix.ruby }} --run "make install test"
        working-directory: ${{ env.WORKING_DIRECTORY }}

  mutate:
    runs-on: [self-hosted]
    env:
      WORKING_DIRECTORY: aggregate_root
      BUNDLE_GEMFILE: ${{ matrix.gemfile }}
      ImageOS: ubuntu22
    strategy:
      fail-fast: false
      matrix:
        include:
          - ruby: ruby-3.1
            gemfile: Gemfile
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: test -e ${{ matrix.gemfile }}.lock
        working-directory: ${{ env.WORKING_DIRECTORY }}
      - run: nix-shell -p ${{ matrix.ruby }} --run "make install mutate-changes"
        working-directory: ${{ env.WORKING_DIRECTORY }}
