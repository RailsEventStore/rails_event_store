# https://github.com/mbj/mutant/blob/master/docs/configuration.md

usage: opensource
requires:
  - rails_event_store
includes:
  - lib
integration:
  name: rspec
mutation:
  operators: light
coverage_criteria:
  process_abort: true
matcher:
  subjects:
    - RailsEventStore*
  ignore:
    - RailsEventStore::Client#initialize
    - RailsEventStore::Client#default_mapper
    - RailsEventStore::Serialization::JSON#preserve_types
    - RailsEventStore::Encryption#initialize
    - RailsEventStore::Encryption#transformations
    - RailsEventStore::Encoding::Base64#transformations
    - RailsEventStore::AsyncHandler.with
